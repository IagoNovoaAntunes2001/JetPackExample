<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>
        <variable
            name="photoItem"
            type="com.example.jetpackandroid.network.model.PhotosItem" />
    </data>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <androidx.cardview.widget.CardView
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_margin="@dimen/list_item_card_view_margin"
            app:cardBackgroundColor="@color/colorLightGrey">

            <TextView
                android:id="@+id/itemTitle"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="center"
                android:layout_margin="@dimen/list_item_text_view_margin"
                android:gravity="center"
                android:padding="@dimen/list_item_text_view_padding"
                android:text="@{photoItem.title}"
                android:textColor="@color/colorWhite"
                android:textSize="@dimen/list_item_text_view_size" />

        </androidx.cardview.widget.CardView>

    </LinearLayout>
</layout>